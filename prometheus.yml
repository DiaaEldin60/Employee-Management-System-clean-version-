scrape_configs:
  - job_name: 'spring-boot-app'
    metrics_path: '/actuator/prometheus'
    scrape_interval: 15s

    static_configs:
      - targets: ['app:8081']   # Actuator management port (Docker Compose service name)
        labels:
          application: ems-backend
          environment: dev